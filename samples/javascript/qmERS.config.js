/*

qmERS.config.js

Configuration settings for Questionmark XPoint public collection
*/

// the URL of XPoint - must end with a '/' character
var g_endpoint = "https://xpoint.questionmark.com/public/statements/";

// notify handler to submit statements using JSONP
var g_notify = "https://xpoint.questionmark.com/public/notify/";

// user name to submit statements
var g_user = "producer";

// the authentication key for this user
var g_accountKey = "nykUv2aroGfYGxwXBofGwGxCVLE61v54";

// the version of the XAPI being used
var g_version = 0.95;

// the default language code
var g_language = "en-US";